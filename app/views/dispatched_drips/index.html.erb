<h1>Listing dispatched_drips</h1>

<table>
  <tr>
    <th>Customer</th>
    <th>Drip</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dispatched_drips.each do |dispatched_drip| %>
  <tr>
    <td><%= dispatched_drip.customer_id %></td>
    <td><%= dispatched_drip.drip_id %></td>
    <td><%= link_to 'Show', dispatched_drip %></td>
    <td><%= link_to 'Edit', edit_dispatched_drip_path(dispatched_drip) %></td>
    <td><%= link_to 'Destroy', dispatched_drip, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dispatched drip', new_dispatched_drip_path %>
