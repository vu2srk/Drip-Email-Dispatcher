<%= form_for(@drip) do |f| %>
  <% if @drip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drip.errors.count, "error") %> prohibited this drip from being saved:</h2>

      <ul>
      <% @drip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
